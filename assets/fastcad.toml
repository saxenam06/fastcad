# What a run reads from this folder. The Input Console shows these choices and lets you change them.

# The CAD a run edits: the production housing, with its ribs. This is the part the drawing draws
# and the part the reference solves were run on, so a variant of it can be compared with something
# real. Chosen 2026-09-16 by measurement: this export behaves the same as the original in every
# operation test, has far fewer defects (self-intersecting pieces 24 -> 1, slivers 23 -> 6, worst
# edge tolerance 0.43 -> 0.27 mm), and it removes the silent 24.6% volume loss when cutting at the
# HSS plane. See docs/grc/rear-housing-254492.md.
#
# Baseline, measured 2026-09-16: 171,777 TET10 elements, 941,550 unknowns, peak displacement
# 0.3658 mm against agenticCAE's recorded 0.3906 mm for its design e56235. See docs/grc/meshing.md.
canvas = "target/cad/254492_prep_small_adv.step"
